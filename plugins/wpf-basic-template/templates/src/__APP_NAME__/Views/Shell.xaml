<mah:MetroWindow x:Class="__APP_NAME__.Views.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:__APP_NAME__.ViewModels"
        mc:Ignorable="d"
        d:DesignWidth="880"
        d:DesignHeight="540"
        d:DataContext="{d:DesignInstance Type=vm:ShellViewModel, IsDesignTimeCreatable=True}"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}"
        TitleBarHeight="48"
        GlowBrush="{DynamicResource AccentColorBrush}"
        WindowStartupLocation="CenterScreen">

    <!-- 标题栏模板：MetroWindow 自带标题栏显示 ShellViewModel.Title（主）+ SubTitle（次），避免与内容区重复 -->
    <mah:MetroWindow.TitleTemplate>
        <DataTemplate>
            <StackPanel VerticalAlignment="Center" Margin="8,0,0,0">
                <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=mah:MetroWindow}, Path=Title}"
                           FontSize="15" FontWeight="SemiBold" />
                <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=mah:MetroWindow}, Path=DataContext.SubTitle}"
                           FontSize="11" Opacity="0.7" />
            </StackPanel>
        </DataTemplate>
    </mah:MetroWindow.TitleTemplate>

    <!-- 窗口 Loaded 后通过 Microsoft.Xaml.Behaviors.Wpf 自带的 InvokeCommandAction 执行 ShellViewModel.LoadedCommand，
         将主内容视图 MainView 区域导航到 ContentRegion（初始导航由 Shell 视图模型处理）。 -->
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <DockPanel LastChildFill="True">
        <!-- 底部状态栏：ShellViewModel.StatusText -->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem Padding="12,4">
                <TextBlock Text="{Binding StatusText}"
                           Opacity="0.75" />
            </StatusBarItem>
        </StatusBar>

        <!-- 主内容区域：由 ShellViewModel.LoadedCommand（Interaction.Triggers）经 Prism 区域导航加载 MainView（导航 URI = "MainView"）。 -->
        <ContentControl prism:RegionManager.RegionName="ContentRegion" />
    </DockPanel>

</mah:MetroWindow>
